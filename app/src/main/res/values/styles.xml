<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lower_blue</item>
        <item name="colorPrimaryDark">@color/upper_blue</item>
        <item name="colorAccent">@color/upper_blue</item>
    </style>

</resources>
